var config = {
    isDebug: true,

    // 默认语法
    defaultDialect: "Complex",

    // 是否生成根元素，如果生成标签为rootName,属性为rootAttr
    isRoot: false,
    rootName: "artical",
    rootAttr: {"class","artical"},

    // 输出位置,空为不进行输出
    output: "#my-article"
}
